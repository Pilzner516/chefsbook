Run this bash command and output the result verbatim with no changes, no reformatting, no line wrapping, no summarizing:

```bash
cat "C:\Users\seblu\aiproj\bob-hq\STATUS.md"
```

Output the raw file content inside a code block so it is not reformatted.
Then add one line after the code block:
"Copy the above and paste into claude.ai to load full project context."
